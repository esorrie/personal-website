import React, { useState } from 'react';
import Tabs from './components/tabs';

const About = () => {

    return (
        <a id="about_section">
            <Tabs/>
        </a>
    );
};
export default About;